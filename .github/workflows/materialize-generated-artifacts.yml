name: Materialize publication reproducibility package

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  placeholder:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Waiting for complete bootstrap payload."
